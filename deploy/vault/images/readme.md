this directory is meant for user-managed images
the "images" directory should be located at `Config::read('physical.path') . '/vault/images/'`
please assure that the web server can write to this directory