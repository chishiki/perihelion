this directory is meant for user-managed files
the "files" directory should be located at `Config::read('physical.path') . '/vault/files/'`
please assure that the web server can write to this directory